// 1559090391822/
function formdate(time) {
  var date = getDate(time)
  console.log(date)
  return date.getFullYear() +'-'+(date.getMonth()+1);
  // return 1234;
}
module.exports = {
  formdate: formdate
}